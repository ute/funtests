fdnonpar
========

An R package for analysis and visualization of x-y-data, such as time series, 
multivariate or functional data.

This package was set up in 2013 as a predecessor to the R-package GET by Mari 
Myllymäki and Tomas Mrkvicka (on CRAN).

You can still install this simple version from R-Forge, use the command
```
install.packages("fdnonpar", repos="http://R-Forge.R-project.org")
```

